<form method="post" action="{{ path('app_persona_delete', {'id': persona.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ persona.id) }}">
    <button class="btn">Delete</button>
</form>
