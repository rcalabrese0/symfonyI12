<form method="post" action="{{ path('app_titulo_delete', {'id': titulo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ titulo.id) }}">
    <button class="btn">Delete</button>
</form>
