<form method="post" action="{{ path('app_estado_delete', {'id': estado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estado.id) }}">
    <button class="btn">Delete</button>
</form>
