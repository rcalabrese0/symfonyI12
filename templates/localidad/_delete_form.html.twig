<form method="post" action="{{ path('app_localidad_delete', {'id': localidad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ localidad.id) }}">
    <button class="btn">Delete</button>
</form>
